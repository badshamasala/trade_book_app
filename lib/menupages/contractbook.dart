import 'package:flutter/material.dart';
class ContractBook extends StatelessWidget {
  const ContractBook({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Contract book'),
      ),
      
    );
  }
}