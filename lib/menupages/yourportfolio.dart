import 'package:flutter/material.dart';
class YourPortfolio extends StatelessWidget {
  const YourPortfolio({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Your portfolio'),
      ),
      
    );
  }
}