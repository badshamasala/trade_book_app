import 'package:flutter/material.dart';
class TradeBookUpgrade extends StatelessWidget {
  const TradeBookUpgrade({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('trade book upgrade'),
      ),
      
    );
  }
}