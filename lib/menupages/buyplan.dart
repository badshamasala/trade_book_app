import 'package:flutter/material.dart';
class BuyPlan extends StatelessWidget {
  const BuyPlan({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Buy Plan'),
      ),
      
    );
  }
}